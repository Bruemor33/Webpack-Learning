require('./style.scss');

const dress = () => {
    document.write("I like my dress a lot, like got damn!");
};

dress();

class Car {
    manufacturer(car) {
        documnet.write(`I have a ${car}`)
    };
};

const bmw = new Car;